<%= @room.name %>

<% @children.each do |child| %>
  <h3>Name : <%= child.name %> </h3>
  <h3> <%= link_to "Edit info", edit_child_path(child.id) %> </h3>
<% end %>
<%= link_to "Add a child", new_user_child_path(@user.id) %>
